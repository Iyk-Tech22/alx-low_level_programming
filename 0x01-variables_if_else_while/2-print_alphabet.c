#include <stdio.h>

/**
 * main - is the entry point to the program
 *
 * Return: 0 when there are no error
 */
int main(void)
{
	int i;

	for (i = 97; i <= 122; i++)
	{
		putchar(i);
	}
	printf("\n");
	return (0);
}
