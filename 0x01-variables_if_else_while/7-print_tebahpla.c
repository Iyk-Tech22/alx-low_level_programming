#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: 0 is if there were no error
 */
int main(void)
{
	int n;

	for (n = 122; n > 96; n--)
		putchar(n);
	putchar('\n');
	return (0);
}
