#include <stdio.h>

/**
 * main - is the entry point of the program
 *
 * Return: 0 if there were no errors
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
